{
  "name": "vivavox",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run start --prefix server\" \"npm run dev --prefix client/web\"",
    "install:all": "npm install --prefix server && npm install --prefix client/web"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
